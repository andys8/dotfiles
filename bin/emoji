#!/bin/bash
API="https://api.getdango.com/api/emoji"
EMOJI=$(zenity --entry --text "Emoji Search:")
[[ $EMOJI ]] && curl -G -s "$API" --data-urlencode "q=$EMOJI" | jq '.results[].text' -r | rofi -dmenu -p "Emoji" -lines 2 -columns 5 -width 15 | xclip -sel clip
